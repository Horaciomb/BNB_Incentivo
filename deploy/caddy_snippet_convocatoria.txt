	# NOTA (07-sep-2026): esto es SOLO el bloque de esta app, no el Caddyfile entero.
	# Antes de llegar aqui el sitio aplica dos matchers globales, anadidos el 05-sep:
	#   @sensibles   -> 404 en *.conf *.env *.ps1 *.pem *.key *pgpass* *.bat *.cmd
	#                   *.pyc *.pyd *.exe *.dll  (sufijo, en TODAS las rutas del sitio)
	#   @no_publicos -> 404 en /cirpob/* /extraer_negocios/* /api/* /frontend/* /mapas/*
	#                   (prefijos DE RAIZ: no alcanzan a /convocatoria/bnb/api/*, verificado)
	# Reponer este bloque no repone esas reglas, ni al reves.

	# -----------------------------------------
	# BNB - Convocatoria Incentivos Cierre de Agosto (FastAPI + React)
	# -----------------------------------------
	handle /convocatoria/bnb/api/* {
		uri strip_prefix /convocatoria/bnb
		reverse_proxy 127.0.0.1:8221 {
			header_up X-Forwarded-Prefix /convocatoria/bnb
			header_up X-Real-IP {remote_host}
			header_down -Server
		}
	}

	redir /convocatoria/bnb /convocatoria/bnb/ 301

	handle_path /convocatoria/bnb/* {
		header {
			-Server
			Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; base-uri 'self'; form-action 'none'; object-src 'none'; frame-ancestors 'none'"
		}
		root * C:/Proyectos/BNB/web/convocatoria
		@deny path *.py *.ps1 *.bat *.env *.conf *.sql *.log *.md *.txt *.bak *.bak.* *.old *.orig *.save *.ejemplo *.gitignore
		handle @deny {
			respond 404
		}
		file_server
		try_files {path} /index.html
	}

