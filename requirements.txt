fastapi==0.136.3
uvicorn==0.49.0
psycopg2-binary==2.9.12
pydantic==2.13.4
python-dotenv

# Lectura de la planilla de Personal publicada (roster de afiliadores).
# OJO: estas versiones estan clavadas a lo que el venv COMPARTIDO
# C:\uv-envs\bnb del servidor YA tenia al 09-sep-2026, no a lo que haya en
# la maquina de desarrollo. Ese venv lo comparten ~8 apps BNB y un
# `uv pip install -r requirements.txt` con un pin distinto las actualiza o
# degrada a todas en su proximo reinicio. Verificar con
# `uv pip list --python C:\uv-envs\bnb\Scripts\python.exe` antes de tocarlas.
pandas==3.0.2
openpyxl==3.1.5
requests==2.33.1
